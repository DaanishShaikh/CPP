#include<bits/stdc++.h>
using namespace std;
int lowerboud(int a; int x);
int main(){
    int n;
    cin>>n;
    int a[n];
    for(int i=0 ; i<n ; i++){
        cin>>a[i];
    }
    a.sort(begin,end);
    int lowerboud(int a,int x){
        int low=0, high=a.size()-1, ans=a.size();
        while(low<high){
           int mid=(low+high)/2;
        }
    }
    
}